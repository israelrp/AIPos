@{
    var grid = Html.DevExpress().GridView(settings =>
    {
        settings.Name = "GridViewListasPrecio";
        settings.CallbackRouteValues = new { Controller = "ListasPrecio", Action = "GridViewPartialListasPrecio" };

        settings.SettingsEditing.AddNewRowRouteValues = new { Controller = "ListasPrecio", Action = "GridViewPartialListasPrecioAddNew" };
        settings.SettingsEditing.UpdateRowRouteValues = new { Controller = "ListasPrecio", Action = "GridViewPartialListasPrecioUpdate" };
        settings.SettingsEditing.DeleteRowRouteValues = new { Controller = "ListasPrecio", Action = "GridViewPartialListasPrecioDelete" };
        settings.SettingsEditing.Mode = GridViewEditingMode.EditFormAndDisplayRow;
        settings.SettingsBehavior.ConfirmDelete = true;

        settings.CommandColumn.Visible = true;
        settings.CommandColumn.NewButton.Visible = true;
        settings.CommandColumn.DeleteButton.Visible = true;
        settings.CommandColumn.EditButton.Visible = true;

        settings.KeyFieldName = "Id";

        settings.SettingsPager.Visible = true;
        settings.Settings.ShowGroupPanel = false;
        settings.Settings.ShowFilterRow = true;
        settings.SettingsBehavior.AllowSelectByRowClick = true;

        settings.Columns.Add("Nombre");
    });
    if (ViewData["EditError"] != null)
    {
        grid.SetEditErrorText((string)ViewData["EditError"]);
    }
}
@grid.Bind(Model).GetHtml()